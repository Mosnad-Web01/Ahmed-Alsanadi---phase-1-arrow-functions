const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;
const add = (x, y) => x + y;
const square = (x) => x * x;

const findSquareRoot = (x) => Math.sqrt(x);
const isEven = (num) => num % 2 === 0;
const calculateCircleArea = (radius_value) =>
	Math.PI * radius_value * radius_value;
